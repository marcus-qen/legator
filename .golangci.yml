run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gosimple

issues:
  exclude-rules:
    - path: '_test\.go'
      linters:
        - errcheck
        - govet
        - staticcheck
        - unused
        - gosimple
