apiVersion: core.infraagent.io/v1alpha1
kind: ModelTierConfig
metadata:
  labels:
    app.kubernetes.io/name: infraagent
    app.kubernetes.io/managed-by: kustomize
  name: modeltierconfig-sample
spec:
  # TODO(user): Add fields here
