---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: userpolicies.legator.io
spec:
  group: legator.io
  names:
    kind: UserPolicy
    listKind: UserPolicyList
    plural: userpolicies
    shortNames:
    - upol
    singular: userpolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.role
      name: Role
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .status.matchedSubjects
      name: Subjects
      priority: 1
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: UserPolicy is the schema for user-scoped authorization policy
          overrides.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UserPolicySpec defines user-scoped authorization policy.
            properties:
              description:
                description: description is a human-readable summary of what this
                  policy grants.
                type: string
              role:
                description: role granted when a subject matches.
                enum:
                - viewer
                - operator
                - admin
                type: string
              scope:
                description: scope constrains where the grant is effective.
                properties:
                  agents:
                    description: |-
                      agents limits to matching agent names.
                      Supports suffix wildcard globs.
                    items:
                      type: string
                    type: array
                  maxAutonomy:
                    description: maxAutonomy caps the maximum autonomy level available
                      to this policy.
                    enum:
                    - observe
                    - recommend
                    - automate-safe
                    - automate-destructive
                    type: string
                  namespaces:
                    description: |-
                      namespaces limits to these Kubernetes namespaces.
                      Empty means no namespace restriction.
                    items:
                      type: string
                    type: array
                  tags:
                    description: |-
                      tags limits to resources tagged with any of these values.
                      Empty means no tag restriction.
                    items:
                      type: string
                    type: array
                type: object
              subjects:
                description: subjects are the identities this policy applies to.
                items:
                  description: UserPolicySubject matches identities via OIDC claims.
                  properties:
                    claim:
                      description: |-
                        claim is the OIDC claim to match.
                        Supported values: email, sub, groups, name.
                      enum:
                      - email
                      - sub
                      - groups
                      - name
                      type: string
                    value:
                      description: |-
                        value is the expected claim value.
                        Supports exact values and suffix wildcard globs (e.g. team-*).
                      type: string
                  required:
                  - claim
                  - value
                  type: object
                minItems: 1
                type: array
            required:
            - role
            - subjects
            type: object
          status:
            description: UserPolicyStatus captures validation and readiness state.
            properties:
              conditions:
                description: conditions represent current reconciliation state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              effectiveRole:
                description: effectiveRole is the reconciled role value.
                enum:
                - viewer
                - operator
                - admin
                type: string
              matchedSubjects:
                description: matchedSubjects is the number of configured subject matchers.
                format: int32
                type: integer
              observedGeneration:
                description: observedGeneration is the generation last processed by
                  the controller.
                format: int64
                type: integer
              ready:
                description: ready indicates whether the policy is valid and usable.
                type: boolean
              validationErrors:
                description: validationErrors contains schema-level validation failures
                  detected by reconciliation.
                items:
                  type: string
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
