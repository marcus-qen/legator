# Keep build context lean while preserving full Go package directories.
# Do NOT blanket-ignore everything; Go builds need directory structure intact.

# VCS / CI metadata
.git
.github
.devcontainer

# Local build artifacts
bin/
main
legator
legator-cli

# Common transient files
*.tar
*.tar.gz
*.log

# Tests are not needed for image build
**/*_test.go
