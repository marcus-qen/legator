apiVersion: v1
kind: Secret
metadata:
  name: legator-probe-config
  namespace: legator-system
  labels:
    app.kubernetes.io/name: legator
    app.kubernetes.io/component: probe
type: Opaque
stringData:
  server-url: "https://legator.lab.k-dev.uk"
  token: "REPLACE_WITH_REGISTRATION_TOKEN"
