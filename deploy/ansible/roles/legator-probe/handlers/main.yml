---
# handlers/main.yml — legator-probe role

- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart legator-probe
  ansible.builtin.systemd:
    name: "{{ legator_probe_service_name }}"
    state: restarted
    daemon_reload: false
